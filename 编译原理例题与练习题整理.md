# 编译原理例题与练习题整理

整理范围：11 份 2026 课件 PDF 中显式出现的例题/示例，以及 `index.html` 导读页中明确列出的练习题。

使用建议：先把每章“练习题”当作空白题独立做；卡住时再回看对应“课件例题”。下面多数条目保留的是“题目要求 + 训练点”，不是完整答案。

---

## 第 1 章 引论

### 课件例题

1. `sum=(10+20)*(num+square);` 的词法分析  
   要求：把源程序字符串分解为 token 序列，例如标识符、赋值号、括号、整常数、加号、乘号、分号等。  
   训练点：词法分析输入/输出、二元组 `(种别码, 属性值)`。

2. `sum=(10+20)*(num+square);` 的中间表示  
   要求：写出后缀表示、前缀表示、四元式、三元式或树表示。  
   训练点：编译阶段之间的数据表示，理解“语义分析/中间代码生成”输出什么。

3. 机器语言、汇编语言、高级语言的同一功能对照  
   要求：理解同一计算任务在不同语言层次的表达差异。  
   训练点：程序设计语言层次，为什么需要翻译程序。

4. T 形图、自展、移植、本机编译器利用  
   要求：用已有编译器构造新编译器，例如在 A 机上构造 B 机 C 编译器，或用 C 编写 NEW 语言编译器。  
   训练点：交叉编译、自展、编译器移植。

### 练习题

1. 区分解释程序、编译程序、汇编程序、交叉编译程序、反编译程序。  
   训练点：概念辨析题。

2. 写出编译器五个主要阶段及每阶段输入输出。  
   训练点：总体结构题。

3. 判断词法分析、语法分析、语义分析、中间代码生成、代码优化、目标代码生成分别属于前端还是后端。  
   训练点：前端/后端边界。

4. 给一行赋值语句，分别写 token 序列、语法成分、中间代码。  
   训练点：完整编译流程。

5. 给一个“已有 A 机 C 编译器，想得到 B 机 C 编译器”的场景，画 T 形图说明步骤。  
   训练点：T 图推理。

---

## 第 2 章 高级语言及其文法

### 课件例题

1. 字母表、字母表乘积、字母表幂  
   例：`Σ1={0,1}`，`Σ2={a,b}`，求 `Σ1Σ2`；求 `{0,1}^3`。  
   训练点：形式语言基础定义。

2. 正闭包和 Kleene 闭包  
   例：写出 `{0,1}+`、`{0,1}*` 的部分元素，注意 `ε` 只在 Kleene 闭包中。  
   训练点：`+` 和 `*` 的区别。

3. 字母表 `{0,1}` 上的语言示例  
   例：`{0,1}`、`{00,11}`、`{00,11}*` 等。  
   训练点：语言是句子集合。

4. 例 2.9：赋值语句的文法  
   题目：为“左部量 = 右部表达式”构造文法，含简单变量、下标变量、运算符等。  
   训练点：写出 `G=(V,T,P,S)`，区分终结符/非终结符/产生式/开始符号。

5. 推导/归约例题：证明 `a=b+m[1]` 是文法 `G1` 的句子  
   要求：从开始符号逐步推导到目标串，或从目标串反向归约到开始符号。  
   训练点：推导、归约、句子归属判定。

6. 判断文法类别  
   例：判断若干文法是正则文法、上下文无关文法、上下文有关文法还是短语结构文法。  
   训练点：Chomsky 体系，0/1/2/3 型文法限制。

7. 构造 `{0123456}` 的左线性文法和右线性文法  
   要求：分别写出左线性与右线性产生式。  
   训练点：正则文法构造，左线性/右线性方向差异。

8. `id+id*id` 的最左推导、最右推导、归约过程  
   文法：`E→E+E|E*E|(E)|id`。  
   训练点：语法树、推导顺序、最左/最右推导。

9. 短语、直接短语、句柄  
   例：对表达式文法 `E→E+T|E-T|T`，`T→T*F|T/F|F`，`F→a1|a2|a3` 和句子 `a1+a2*a3`，找推导过程中句型的短语、直接短语、句柄。  
   训练点：自底向上分析前置概念。

10. 二义性例题：`id+id*id`  
    文法：`E→E+E|E*E|(E)|id`。  
    要求：用两棵语法树或两条不同最左推导说明文法二义。  
    训练点：二义性证明。

11. `if-then-else` 悬挂 else 二义性  
    要求：说明 `if e1 then if e2 then s1 else s2` 的两种归属，并理解 matched/unmatched 文法。  
    训练点：二义性消除与实际语言设计。

### 练习题

1. 构造所有以 `a` 开头、以 `b` 结尾的字符串的文法。  
   训练点：格式约束。

2. 构造所有二进制偶数的文法。  
   训练点：末位约束、是否允许前导 0。

3. 构造所有长度至少为 2 的十进制数字串的文法。  
   训练点：长度约束。

4. 构造所有不包含 `00` 的二进制串的文法。  
   训练点：局部禁忌约束。

5. 构造 `L={a^n b^n | n>=0}` 或 `n>=1` 的文法。  
   训练点：数量配对、递归包裹。

6. 构造 `L={a^n b^n c^m}` 的文法。  
   训练点：一部分数量相关，一部分自由重复。

7. 构造括号匹配语言的文法。  
   训练点：嵌套与并列。

8. 构造回文串文法。  
   训练点：对称递归。

9. 给一个文法和一个句子，写最左推导、最右推导、归约过程。  
   训练点：推导/归约基本功。

10. 给语法树，找短语、直接短语、句柄。  
    训练点：句柄识别。

11. 给文法 `E→E+E|E*E|(E)|id`，证明它二义；再写出体现优先级的无二义表达式文法。  
    训练点：二义性与优先级。

---

## 第 3 章 词法分析

### 课件例题

1. 例 3.1：`if count>7 then result := 3.14;` 的单词符号序列  
   要求：写出关键字、标识符、关系运算符、常数、赋值号、分号等 token。  
   训练点：token 二元组，关键字与标识符区分。

2. 例 3.2：标识符的文法  
   例：`sum`、`result`、`a1`、`b2`。  
   要求：用产生式描述以字母开头、后接字母或数字的标识符。  
   训练点：正则文法描述 token。

3. 标识符的正则表达式  
   例：`letter(letter|digit)*`。  
   训练点：正则表达式与正则文法等价。

4. 正则表达式语言示例  
   例：`L(a|b)`、`L((a|b)(a|b))`、`L((a|b)*)`。  
   训练点：正则运算语义。

5. 例 3.6：把正则文法转换成正则表达式  
   文法含 `S→aS|aB`、`B→bB|bC|aB|bS`、`C→cC|c`。  
   训练点：由方程组/代数化方法求正则表达式。

6. 标识符的正则定义式  
   要求：先定义 `letter`、`digit`，再定义 `id`。  
   训练点：正则定义的分层写法。

7. 正则表达式 `a(a|b)*` 转正则文法  
   训练点：正则式到右线性文法。

8. 例 3.10：标识符定义的转换  
   要求：把正则定义改写为可用于构造自动机/文法的形式。  
   训练点：正则定义展开。

9. 例 3.7：某简易语言的词法规则  
   要求：根据正则定义式描述各类 token。  
   训练点：完整词法规则表。

10. 奇偶校验器 DFA  
    要求：识别输入串中 `1` 的个数为奇数的语言。  
    训练点：有限状态机设计。

11. DFA 运行例题  
    给定 DFA `M` 和输入串 `w=baa`，计算扩展转移结果。  
    训练点：DFA 接受判定。

12. 构造 DFA：`Σ={0,1}`，`L={x | x 中 0 的个数和 1 的个数都是偶数}`  
    训练点：状态含义设计，四状态 DFA。

13. 构造正则式 `ε|(0|1)01*|0+` 的状态转换图  
    训练点：正则式到 NFA/状态转换图。

14. 例 3.14：不同进制无符号整数的识别  
    要求：识别八进制、十进制、十六进制无符号整数。  
    训练点：实际 token 的状态图设计。

15. Fortran 中 `DO100I=1,5`、`IF(5.EQ.M)X=5` 的关键字识别  
    训练点：超前扫描、关键字与标识符冲突。

16. 词法分析程序数据结构与子例程  
    例：`ch`、`token`、`install_id(token)`、`getchar()` 等。  
    训练点：手写词法分析器实现。

### 练习题

1. 输入串：`if count<=10 then count:=count+1`，写 token 序列。  
   训练点：关键字、标识符、关系运算符、赋值号。

2. 为正则式 `a(a|b)*b` 写右线性文法。  
   训练点：正则式转文法。

3. NFA 子集构造法：  
   NFA 状态 `q0`、`q1`，初态 `q0`，终态 `q1`；转移 `q0 --a--> q0`、`q0 --a--> q1`、`q1 --b--> q1`。构造 DFA 转移表。  
   训练点：NFA 确定化。

4. 给正则表达式，构造 NFA，再确定化、最小化。  
   训练点：自动机综合题。

5. 给状态转换图，写等价右线性文法。  
   训练点：自动机与正则文法互转。

6. 给右线性文法，画状态转换图。  
   训练点：正则文法到自动机。

7. 为整数、实数、注释、字符串分别画状态转换图。  
   训练点：典型 token 识别。

---

## 第 4 章 自顶向下的语法分析

### 课件例题

1. 自顶向下分析例题：`S→xAy`，`A→**|*`，输入串 `x**y`  
   训练点：从开始符号推导输入串，理解试探分析。

2. 输入串 `id+id*id` 的最左推导  
   训练点：回溯问题、表达式文法二义性对自顶向下分析的影响。

3. 表达式文法的 FIRST 集  
   文法：`E→TE'`，`E'→+TE'|ε`，`T→FT'`，`T'→*FT'|ε`，`F→(E)|id`。  
   训练点：FIRST 集计算。

4. 表达式文法的 FOLLOW 集  
   同上文法，求 `FOLLOW(E)`、`FOLLOW(E')`、`FOLLOW(T)`、`FOLLOW(T')`、`FOLLOW(F)`。  
   训练点：FOLLOW 集规则。

5. 非 LL(1) 文法例题：`S→cAd`，`A→ab|a`  
   输入 `cad` 出现不确定性。  
   训练点：候选式 FIRST 集相交、左因子问题。

6. 例 4.10：简单算术表达式文法的预测分析实现  
   要求：构造文法、消除左递归、求 FIRST/FOLLOW/SELECT、构造预测分析表。  
   训练点：LL(1) 全流程。

7. 例 4.14：产生式 `E'→+TE'` 的递归下降子程序  
   训练点：非终结符对应子程序。

8. 例 4.15：简单表达式文法的语法图  
   训练点：语法图与递归子程序法。

9. 例 4.16：简单算术表达式的语法分析器  
   训练点：递归下降分析器实现。

### 练习题

1. 消除表达式文法的直接左递归：  
   `E→E+T|T`，`T→T*F|F`，`F→(E)|id`。  
   训练点：左递归消除。

2. 求 FIRST、FOLLOW、SELECT：  
   `S→aA|b`，`A→c|ε`，结束符为 `#`。  
   训练点：集合计算与 LL(1) 判断。

3. 对一个给定文法判断是否 LL(1)。  
   要求：列 FIRST/FOLLOW/SELECT，同左部 SELECT 两两不交才是 LL(1)。  
   训练点：LL(1) 判定标准。

4. 构造 LL(1) 预测分析表并模拟输入串分析过程。  
   训练点：表驱动预测分析。

5. 将含公共左因子的文法改写为 LL(1) 候选文法。  
   训练点：提取左因子。

6. 根据 LL(1) 文法写递归下降分析程序伪代码。  
   训练点：程序实现题。

---

## 第 5 章 自底向上的语法分析

### 课件例题

1. 例 5.1：简单归约过程  
   文法：`S→aABe`，`A→Abc|b`，`B→d`；句子：`abbcde`。  
   要求：从输入串归约到 `S`。  
   训练点：自底向上分析、句柄归约。

2. 例 5.2：`E→E+E|E*E|(E)|id` 的移进-归约分析  
   输入形如 `id1+id2*id3`。  
   训练点：移进、归约、用产生式序列表示分析树。

3. 状态法示例：`S→bBB` 的项目推进  
   例：`S→.bBB`、`S→b.BB`、`S→bB.B`、`S→bBB.`。  
   训练点：LR 项目中圆点含义。

4. 例 5.6：表达式文法的算符优先关系  
   要求：构造终结符之间的 `<·`、`=·`、`>·` 关系表。  
   训练点：FIRSTOP、LASTOP 与优先矩阵。

5. 例 5.7：用算符优先分析法分析 `id+id`  
   文法：`E→E+T|E-T|T`，`T→T*F|T/F|F`，`F→(E)|id`。  
   训练点：算符优先分析过程表。

6. 素短语例题：句型 `T+T*F+id`  
   要求：找短语、素短语、最左素短语。  
   训练点：算符优先分析中的归约对象。

7. 例 5.10：优先函数 `f`、`g`  
   要求：由算符优先关系构造优先函数。  
   训练点：优先函数法。

8. 活前缀例题：规范句型 `aAbcde`  
   要求：找可归前缀、活前缀。  
   训练点：规范句型活前缀。

9. 例 5.12：LR 分析表  
   文法：`S→BB`，`B→aB|b`。  
   要求：根据 DFA 填 ACTION/GOTO 表，并用表分析输入串。  
   训练点：LR(0) 分析表使用。

10. 例 5.13：构造 LR(0) 项目集规范族和 DFA  
    增广文法：`S'→S`，`S→BB`，`B→aB|b`。  
    要求：求全部 LR(0) 项目、CLOSURE、GOTO、项目集 DFA。  
    训练点：LR(0) 表构造基础。

11. LR(0) 冲突例题  
    文法：`S→A|B`，`A→aAc|a`，`B→bBd|b`。  
    要求：观察项目集中移进-归约冲突。  
    训练点：LR(0) 不是总有效。

12. SLR(1)、LR(1)、LALR(1) 分析表构造  
    要求：在 LR(0) 项目集基础上使用 FOLLOW 或向前看符号降低冲突。  
    训练点：LR 家族区别。

### 练习题

1. 给文法和输入串，写移进-归约分析过程。  
   训练点：栈、输入、动作三列表。

2. 求 FIRSTOP、LASTOP，并构造算符优先矩阵。  
   训练点：算符优先题。

3. 用算符优先分析法分析一个表达式输入串。  
   训练点：找最左素短语。

4. 构造增广文法、LR(0) 项目集规范族、DFA、ACTION/GOTO 表。  
   训练点：LR 大题标准流程。

5. 判断 LR(0) 冲突和 SLR 修正。  
   训练点：移进-归约冲突、归约-归约冲突、FOLLOW 的作用。

6. 构造 LR(1) 项目集，比较 LR(1) 与 SLR(1) 的不同。  
   训练点：向前看符号。

7. 合并同心 LR(1) 项目集得到 LALR(1) 表。  
   训练点：LALR 构造。

---

## 第 6 章 语法制导翻译与属性文法

### 课件例题

1. 例 6.1：台式计算器的语法制导定义  
   例：`L→E n {print(E.val)}`，`E→E1+T {E.val:=E1.val+T.val}`。  
   训练点：综合属性计算表达式值。

2. 例 6.2：用继承属性解决不匹配问题  
   训练点：继承属性从父结点/左兄弟传递信息。

3. 例 6.3：注释分析树的依赖图  
   要求：为属性实例建结点，为依赖关系画有向边。  
   训练点：属性计算顺序、循环依赖判断。

4. L-属性定义示例  
   例：`T→F T'` 中 `T'.inh:=F.val`，`T.val:=T'.syn`。  
   训练点：L-属性定义的约束。

5. 6.3.5 属性计算示例：构造表达式抽象语法树  
   要求：用 `mknode`、`mkleaf` 等函数构造 AST。  
   训练点：抽象语法树作为中间表示。

6. 例 6.10：简单翻译模式  
   题目：将中缀表达式翻译成后缀表达式。  
   训练点：语义动作插入位置。

7. 例 6.11：从 L-属性定义建立翻译模式  
   语言：数学排版语言 EQN。  
   训练点：继承属性动作必须放在被依赖符号前。

8. 例 6.14：用 LR 分析器实现台式计算器  
   训练点：自底向上归约时执行综合属性动作。

9. 例 6.15：左递归翻译模式处理中缀转后缀  
   例：`E→E1+T {print('+')}`。  
   训练点：左递归消除时保留语义动作含义。

10. 例 6.17：对输入串 `3*x/y` 的翻译  
    要求：使用分析栈和语义栈执行翻译。  
    训练点：L-属性定义的自底向上翻译技巧。

11. 例 6.18：数学排版语言 EQN  
    训练点：继承属性在自底向上分析中的实现。

### 练习题

1. 给表达式文法，写出计算表达式值的 SDD。  
   训练点：综合属性。

2. 给带类型声明的文法，写出类型传递的 SDD。  
   训练点：继承属性。

3. 给注释分析树，画属性依赖图并给出求值顺序。  
   训练点：依赖图拓扑排序。

4. 判断一个属性定义是不是 S-属性定义或 L-属性定义。  
   训练点：属性分类。

5. 为 `3*x/y` 构造抽象语法树。  
   训练点：AST 构造。

6. 将中缀表达式翻译为后缀表达式，写翻译模式。  
   训练点：SDT 语义动作位置。

7. 把一个 S-属性定义改写成 LR 分析时执行的语义动作。  
   训练点：自底向上属性计算。

---

## 第 7 章 语义分析与中间代码生成

### 课件例题

1. 例 7.1：中缀、前缀、后缀表示互转  
   例：`a+b`、`a*(b+c)`、`(a+b)*(c+d)`、`a:=a*b+c*d`。  
   训练点：逆波兰表示、运算顺序。

2. 例 7.2：`a:=(-b)*(c+d)-(c+d)` 的三地址码  
   训练点：临时变量生成、表达式翻译。

3. 例 7.3：表达式 `a+a*(b-c)-(b-c)/d` 的 DAG  
   训练点：公共子表达式 `b-c` 的共享。

4. 声明 `x:real; i:integer` 的翻译  
   要求：向符号表登记类型和 offset，更新 offset。  
   训练点：声明语句翻译、宽度计算。

5. 嵌套 Pascal 程序 `sort` 的声明翻译  
   训练点：嵌套过程、作用域、符号表。

6. 记录类型示例：`struct Node { float x, y; struct node *next; }`  
   训练点：记录字段符号表、字段偏移、对齐问题。

7. 数组寻址例题：`A` 是 `10×20` 整型数组  
   要求：计算二维数组元素地址/偏移，并生成三地址码。  
   训练点：数组元素寻址、行优先/维度跨度。

8. 例 7.8：`a<b or c<d and e<f` 的数值翻译  
   训练点：布尔表达式求值、临时变量。

9. 例 7.9：`a<b or c<d and e<f` 的控制流翻译  
   训练点：短路求值、真出口/假出口。

10. 例 7.10：翻译  
    `while a < b do if c < d then x := y+z else x := y-z`。  
    训练点：while、if-then-else 的目标代码结构。

11. 混合模式布尔表达式：`4+a>b-c and d`  
    训练点：算术值与布尔值混合时的类型与控制流翻译。

12. 例 7.11：布尔表达式回填  
    训练点：`truelist`、`falselist`、`makelist`、`merge`、`backpatch`。

13. 例 7.12：回填式翻译嵌套语句  
    语句：`while a < b do if c < 5 then while x > y do z := x + 1 else x := y`。  
    训练点：`nextlist`、嵌套控制结构回填。

14. 例 7.14：翻译 switch 语句  
    训练点：case 表、跳转结构、控制流。

### 练习题

1. 声明语句的 offset 计算：  
   设 `int` 宽度 4，`real` 宽度 8；声明序列 `int a; real b; int c;`，初始 `offset=0`。  
   训练点：符号表 offset。

2. 二维数组地址计算：  
   数组 `A[1:3,0:4]` 按行优先存放，元素宽度 4。求 `A[i,j]` 相对位移，并计算 `A[2,3]`。  
   训练点：数组地址公式。

3. `if a < b then x := 1 else x := 2` 的三地址码。  
   训练点：条件跳转、标号。

4. `y := f(a, b+1)` 的过程调用三地址码，假设实参从左到右求值。  
   训练点：`param`、`call`、返回值。

5. 给表达式写三地址码、四元式、三元式。  
   训练点：中间代码形式互转。

6. 给布尔表达式写数值翻译和控制流翻译。  
   训练点：短路求值。

7. 用回填法翻译 `while`、`if-then-else`、`for`、`repeat`。  
   训练点：控制结构综合题。

8. 判断类型等价：名字等价、结构等价。  
   训练点：类型检查。

---

## 第 8 章 符号表管理

### 课件例题

1. 符号表基本格式  
   要求：说明表项中名字和属性信息如何组织。  
   训练点：符号表记录什么。

2. 字符串表管理名字  
   要求：比较固定长度名字字段、可变长度名字字段、单独字符串表。  
   训练点：空间效率和查找效率。

3. 多种符号共用符号表的实现结构  
   训练点：不同符号类别具有不同属性集合。

4. 用扩展属性链组织函数形参符号表  
   训练点：复杂属性信息组织。

5. 散列表实现符号表  
   训练点：散列函数、冲突处理、查找效率。

6. C 语言程序块实例及其符号表  
   要求：为嵌套程序块分别建立符号表，处理同名遮蔽。  
   训练点：块结构作用域。

7. 完整 C 程序符号表、嵌套过程符号表  
   训练点：多层作用域、符号表之间的链接。

### 练习题

1. 说明符号表在词法分析、语义分析、中间代码生成、目标代码生成中的作用。  
   训练点：符号表全局作用。

2. 给一段含嵌套 `{}` 的 C 代码，画每个程序块对应的符号表。  
   训练点：作用域和遮蔽。

3. 给标识符集合，分别用线性表和散列表组织，比较查找过程。  
   训练点：符号表组织结构。

4. 给函数声明，设计符号表表项中的属性字段。  
   训练点：类型、宽度、参数、返回值、地址属性。

5. 判断同一符号表中是否允许同名，不同嵌套作用域中如何处理同名。  
   训练点：声明唯一性检查。

---

## 第 9 章 运行时的存储组织

### 课件例题

1. 静态作用域示例  
   C 代码中含多层块和重复声明 `a`、`b`，要求判断每个 `printf` 引用哪个声明。  
   训练点：词法作用域、名字遮蔽。

2. 名字、变量、标识符的区别  
   例：`x.y` 中 `x` 所代表结构的域 `y`。  
   训练点：编译时名字与运行时位置。

3. 静态存储分配策略  
   例：Fortran 这类不允许递归、无可变体积数据对象的语言。  
   训练点：何时编译期可确定存储布局。

4. 动态/栈式存储分配策略  
   例：C 语言允许过程递归，编译时不能确定活动记录数量。  
   训练点：活动记录、控制栈。

5. 调用序列和返回序列  
   要求：理解参数、返回地址、老 `sp`、动态链等字段如何保存。  
   训练点：过程调用实现。

6. 访问动态分配数组  
   训练点：栈中可变长数据。

7. 内存体系和局部性示例  
   训练点：堆管理、缓存局部性。

### 练习题

1. 访问链与控制链：  
   过程嵌套关系为 `P` 中定义 `Q`，`Q` 中定义 `R`。当前在 `R` 中，访问 `P` 的局部变量 `x`。  
   训练点：静态链/访问链与动态链/控制链区别。

2. 给一段嵌套过程调用序列，画活动树和控制栈。  
   训练点：过程活动。

3. 画活动记录布局，标出参数、返回地址、动态链、静态链、局部变量、临时变量。  
   训练点：运行时栈帧。

4. 判断静态分配、栈式分配、堆分配分别适合哪些数据对象。  
   训练点：生命周期与存储区。

5. 比较传值、传地址、传引用、传名等参数传递方式。  
   训练点：参数传递。

6. 给堆空闲块列表和申请序列，用 first-fit/best-fit 分配。  
   训练点：堆空间管理。

---

## 第 10 章 代码优化

### 课件例题

1. 公共子表达式删除  
   要求：若表达式先前已计算且操作数未改变，则复用结果。  
   训练点：局部/全局公共子表达式。

2. 复制传播  
   例：`f := g` 后尽量用 `g` 替换 `f`。  
   训练点：复制传播为死代码删除创造机会。

3. 无用代码删除  
   要求：删除计算结果以后不再被引用的语句。  
   训练点：活跃变量、副作用判断。

4. 代码外提  
   要求：把循环不变计算移到循环前。  
   训练点：循环优化。

5. 强度削弱  
   例：`x^2` 改为 `x*x`，`2*x` 改为 `x+x`，多项式按 Horner 形式计算。  
   训练点：等价变换与代价降低。

6. 基本块示例  
   课件中连续三地址语句 `t1:=a*a`、`t2:=a*b`、`t3:=2*t2`、`t4:=t1+t3`、`t5:=b*b`、`t6:=t4+t5`。  
   训练点：基本块内优化。

### 练习题

1. 划分基本块：  
   ```
   1  t1 := a + b
   2  if t1 < c goto 5
   3  d := t1 * 2
   4  goto 6
   5  d := t1 + 1
   6  e := d
   ```
   训练点：入口语句、基本块、流图。

2. 基本块内公共子表达式优化：  
   ```
   t1 := a + b
   t2 := a + b
   t3 := t2 * c
   d := t1
   e := d * c
   ```
   出口活跃变量只有 `d` 和 `e`。  
   训练点：公共子表达式、死代码删除。

3. 给一段三地址码，做常量传播、复制传播、死代码删除。  
   训练点：局部优化组合。

4. 给循环三地址码，找循环不变式并做代码外提。  
   训练点：循环优化。

5. 给循环中的归纳变量，做强度削弱和归纳变量删除。  
   训练点：循环优化大题。

6. 根据基本块画 DAG，并由 DAG 重建优化后的三地址码。  
   训练点：DAG 优化。

---

## 第 11 章 代码生成

### 课件例题

1. 三地址语句 `x := y + z` 的朴素目标代码  
   例：`MOV y, R0`，`ADD z, R0`，`MOV R0, x`。  
   训练点：指令选择。

2. 语句序列 `a := b + c; d := a + e` 的低质量代码  
   要求：发现 `a` 若后续不再使用，则存回/重新装入可能冗余。  
   训练点：局部代码生成质量。

3. `a := a + 1` 使用特殊指令 `INC a`  
   训练点：目标机特殊指令。

4. 寄存器分配与寄存器指派  
   要求：区分“哪些变量驻留在寄存器”和“具体用哪个寄存器”。  
   训练点：后端核心难点。

5. 窥孔优化：冗余指令消除  
   例：`MOV R0, a` 后接 `MOV a, R0`，若第二条无标号且无必要，可以删除。  
   训练点：短窗口等价替换。

6. 窥孔优化：不可达代码消除  
   要求：删除无条件跳转后面、且无标号可跳入的指令。  
   训练点：目标代码控制流优化。

7. 窥孔优化：强度削弱、特殊机器指令、代数化简  
   例：`x*x` 替代指数过程，移位实现乘/除 2，删除 `x:=x+0`、`x:=x*1`。  
   训练点：机器相关优化。

### 练习题

1. 窥孔优化：  
   ```
   MOV a, R0
   MOV R0, b
   MOV b, R0
   ADD c, R0
   MOV R0, d
   ```
   要求：判断哪条装入冗余，并写优化后代码。  
   训练点：冗余指令删除。

2. 给三地址码序列，翻译成目标指令序列。  
   训练点：装入、运算、存储。

3. 给目标代码片段，删除不可达代码和连续跳转。  
   训练点：控制流窥孔优化。

4. 给若干表达式目标代码，使用强度削弱和代数化简。  
   训练点：等价替换。

5. 给有限寄存器数量，决定哪些临时值保留在寄存器，哪些溢出到内存。  
   训练点：寄存器分配直觉。

---

## 总复习题型索引

1. 文法构造题：输出产生式集合。  
   常见来源：第 2 章、第 3 章。

2. 二义性证明题：输出两棵语法树或两条不同最左/最右推导。  
   常见来源：第 2 章。

3. 自动机题：正则式、正则文法、NFA、DFA、最小化 DFA 互转。  
   常见来源：第 3 章。

4. LL(1) 题：FIRST、FOLLOW、SELECT、预测分析表、分析过程。  
   常见来源：第 4 章。

5. LR 题：增广文法、项目集规范族、DFA、ACTION/GOTO 表、冲突判断。  
   常见来源：第 5 章。

6. 属性文法题：SDD、依赖图、S/L 属性定义、翻译模式。  
   常见来源：第 6 章。

7. 中间代码题：三地址码、四元式、数组寻址、控制流翻译、回填。  
   常见来源：第 7 章。

8. 符号表题：表项属性、作用域、散列表、嵌套符号表。  
   常见来源：第 8 章。

9. 运行时存储题：活动记录、调用序列、返回序列、访问链、控制链、堆管理。  
   常见来源：第 9 章。

10. 优化题：基本块、流图、公共子表达式、复制传播、死代码、循环优化。  
    常见来源：第 10 章。

11. 代码生成题：三地址码到目标代码、寄存器、窥孔优化。  
    常见来源：第 11 章。
